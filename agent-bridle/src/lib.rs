//! `agent-bridle` — the facade.
//!
//! Bootstrap scaffold. The P0 facade (`registry()` building the default tool
//! set via the explicit builder, plus tool re-exports) lands via the
//! `p0/core-and-shell` pull request. See `docs/DESIGN.md` §4/§5.

#![forbid(unsafe_code)]
