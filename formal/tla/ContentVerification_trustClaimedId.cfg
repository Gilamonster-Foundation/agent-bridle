\* THE GAP: a CID computed/stored but never checked. TLC MUST report a violation
\* (claimedCid=oNarrow over payload=oWide accepted) — a forged identity. Documents
\* why merely COMPUTING a CID is not security.
SPECIFICATION Spec
CONSTANT Design = "TrustClaimedId"
CONSTANT Objects = {oNarrow, oWide}
INVARIANT Inv
