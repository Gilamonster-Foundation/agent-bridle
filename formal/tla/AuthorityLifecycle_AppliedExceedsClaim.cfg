\* BUG mode "AppliedExceedsClaim": TLC MUST report an Inv violation (counterexample) — it
\* breaks T3. Documents a design bridle must NOT ship.
\* Run:  tlc -config AuthorityLifecycle_AppliedExceedsClaim.cfg AuthorityLifecycle.tla
SPECIFICATION Spec
CONSTANT Mode = "AppliedExceedsClaim"
CONSTANT Resources = {r1, r2}
CONSTANT Secrets = {s1}
CONSTANT ExplicitDelegation = {}
INVARIANT Inv
