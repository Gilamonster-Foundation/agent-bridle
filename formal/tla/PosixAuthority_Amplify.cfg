\* BUG mode "Amplify": TLC MUST report an Inv violation (counterexample) — it
\* breaks NoAuthorityAmplification. Documents a POSIX projection bridle must NOT ship.
\* Run: tlc -config PosixAuthority_Amplify.cfg PosixAuthority.tla
SPECIFICATION Spec
CONSTANT Mode = "Amplify"
CONSTANT Objects = {o1, o2}
CONSTANT Rights = {r, w}
INVARIANT Inv
