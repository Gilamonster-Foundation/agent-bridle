\* BUG mode "SilentWiden": TLC MUST report an Inv violation (counterexample) — it
\* breaks T2. Documents a design bridle must NOT ship.
\* Run:  tlc -config AuthorityLifecycle_SilentWiden.cfg AuthorityLifecycle.tla
SPECIFICATION Spec
CONSTANT Mode = "SilentWiden"
CONSTANT Resources = {r1, r2}
CONSTANT Secrets = {s1}
CONSTANT ExplicitDelegation = {}
INVARIANT Inv
