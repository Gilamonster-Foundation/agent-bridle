\* BUG mode "SubstituteEvidence": TLC MUST report an Inv violation (counterexample) — it
\* breaks T7. Documents a design bridle must NOT ship.
\* Run:  tlc -config AuthorityLifecycle_SubstituteEvidence.cfg AuthorityLifecycle.tla
SPECIFICATION Spec
CONSTANT Mode = "SubstituteEvidence"
CONSTANT Resources = {r1, r2}
CONSTANT Secrets = {s1}
CONSTANT ExplicitDelegation = {}
INVARIANT Inv
