\* BUG mode "DescendantEscalate": TLC MUST report an Inv violation (counterexample) — it
\* breaks T5. Documents a design bridle must NOT ship.
\* Run:  tlc -config AuthorityLifecycle_DescendantEscalate.cfg AuthorityLifecycle.tla
SPECIFICATION Spec
CONSTANT Mode = "DescendantEscalate"
CONSTANT Resources = {r1, r2}
CONSTANT Secrets = {s1}
CONSTANT ExplicitDelegation = {}
INVARIANT Inv
