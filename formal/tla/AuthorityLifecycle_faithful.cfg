\* FAITHFUL model: the full chain honors T1..T7. TLC MUST report []Inv holds.
\* Run:  tlc -config AuthorityLifecycle_faithful.cfg AuthorityLifecycle.tla
SPECIFICATION Spec
CONSTANT Mode = "Faithful"
CONSTANT Resources = {r1, r2}
CONSTANT Secrets = {s1}
CONSTANT ExplicitDelegation = {}
INVARIANT Inv
