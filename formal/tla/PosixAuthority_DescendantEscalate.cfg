\* BUG mode "DescendantEscalate": TLC MUST report an Inv violation (counterexample) — it
\* breaks DescendantAttenuation. Documents a POSIX projection bridle must NOT ship.
\* Run: tlc -config PosixAuthority_DescendantEscalate.cfg PosixAuthority.tla
SPECIFICATION Spec
CONSTANT Mode = "DescendantEscalate"
CONSTANT Objects = {o1, o2}
CONSTANT Rights = {r, w}
INVARIANT Inv
