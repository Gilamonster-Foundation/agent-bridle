\* BUG mode "ExecBeforeAdmit": TLC MUST report an Inv violation (counterexample) — it
\* breaks T1. Documents a design bridle must NOT ship.
\* Run:  tlc -config AuthorityLifecycle_ExecBeforeAdmit.cfg AuthorityLifecycle.tla
SPECIFICATION Spec
CONSTANT Mode = "ExecBeforeAdmit"
CONSTANT Resources = {r1, r2}
CONSTANT Secrets = {s1}
CONSTANT ExplicitDelegation = {}
INVARIANT Inv
