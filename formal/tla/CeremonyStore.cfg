\* TLC model for the P2 chain-store (CAS append + anti-rollback).
\* Run:  java -cp tla2tools.jar tlc2.TLC -config CeremonyStore.cfg CeremonyStore.tla
\* Expect: no error; Inv holds; CheckpointMono and LenMono hold.
SPECIFICATION Spec
CONSTANT RecordIds = {r1, r2, r3}
INVARIANT Inv
PROPERTY CheckpointMono
PROPERTY LenMono
CONSTRAINT StateBound
