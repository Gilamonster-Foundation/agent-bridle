\* FAITHFUL model: the POSIX projection honors all four properties. TLC MUST
\* report []Inv holds.  Run: tlc -config PosixAuthority_faithful.cfg PosixAuthority.tla
SPECIFICATION Spec
CONSTANT Mode = "Faithful"
CONSTANT Objects = {o1, o2}
CONSTANT Rights = {r, w}
INVARIANT Inv
