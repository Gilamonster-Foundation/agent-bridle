\* BUG mode "DescriptorForge": TLC MUST report an Inv violation (counterexample) — it
\* breaks DescriptorNonAmplification. Documents a POSIX projection bridle must NOT ship.
\* Run: tlc -config PosixAuthority_DescriptorForge.cfg PosixAuthority.tla
SPECIFICATION Spec
CONSTANT Mode = "DescriptorForge"
CONSTANT Objects = {o1, o2}
CONSTANT Rights = {r, w}
INVARIANT Inv
