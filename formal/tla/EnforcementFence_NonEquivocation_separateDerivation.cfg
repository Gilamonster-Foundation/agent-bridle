\* THE #317 BUG: spawn re-derives a possibly-wider fence. TLC MUST report an Inv
\* violation (trace: Admit resolved={r1} under authorized={r1}; Exec applies
\* {r1,r2} -> L2 and L3 both fail) = sh->bash. Documents the design bridle must NOT ship.
SPECIFICATION Spec
CONSTANT Design = "SeparateDerivation"
CONSTANT Resources = {r1, r2}
INVARIANT Inv
