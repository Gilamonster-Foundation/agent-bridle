\* FailClosed: acceptance re-checks the exact grant/fence binding AND the session
\* epoch, so a stale post-crash replay is refused. []Inv holds.
SPECIFICATION Spec
INVARIANT Inv
CONSTANTS
  Grants = {g1, g2}
  Fences = {f1, f2}
  MaxEpoch = 2
  Design = "FailClosed"
