\* BUG mode "UnknownAdmit": TLC MUST report an Inv violation (counterexample) — it
\* breaks T4. Documents a design bridle must NOT ship.
\* Run:  tlc -config AuthorityLifecycle_UnknownAdmit.cfg AuthorityLifecycle.tla
SPECIFICATION Spec
CONSTANT Mode = "UnknownAdmit"
CONSTANT Resources = {r1, r2}
CONSTANT Secrets = {s1}
CONSTANT ExplicitDelegation = {}
INVARIANT Inv
