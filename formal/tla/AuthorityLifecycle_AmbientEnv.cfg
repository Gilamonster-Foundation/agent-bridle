\* BUG mode "AmbientEnv": TLC MUST report an Inv violation (counterexample) — it
\* breaks T6. Documents a design bridle must NOT ship.
\* Run:  tlc -config AuthorityLifecycle_AmbientEnv.cfg AuthorityLifecycle.tla
SPECIFICATION Spec
CONSTANT Mode = "AmbientEnv"
CONSTANT Resources = {r1, r2}
CONSTANT Secrets = {s1}
CONSTANT ExplicitDelegation = {}
INVARIANT Inv
