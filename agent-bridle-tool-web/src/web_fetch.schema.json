{
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "description": "The http(s) URL to fetch. Its host (and every redirect host) is gated by the `net` caveat and SSRF-screened."
    },
    "max_bytes": {
      "type": "integer",
      "minimum": 1,
      "description": "Optional cap on response bytes read before extraction. The `maximum` is the HARD_MAX_BYTES ceiling, injected at runtime."
    }
  },
  "required": ["url"],
  "additionalProperties": false
}
